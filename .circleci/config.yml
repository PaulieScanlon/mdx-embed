version: 2.1
orbs:
  coveralls: coveralls/coveralls@1.0.6
jobs:
  steps:
    - checkout
    - run:
        name: collect-coverage
        yarn: true
        command: yarn install && make test-coverage
    - coveralls/upload
