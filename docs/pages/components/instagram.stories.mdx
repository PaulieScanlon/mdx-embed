import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { Instagram } from '../../../packages/mdx-embed/src/components/instagram';

<Meta title="Components/Instagram" component={Instagram} />

# Instagram

Display an Instagram post by including the component in your `.mdx`. The `instagramId` prop is required.

<ArgsTable of={Instagram} />

## Usage

<Canvas>
  <Story name="usage">
    <Instagram instagramId="B7ISOoHne6s" />
  </Story>
</Canvas>

## Help

To embed an Instagram post using the `<Instagram />` component grab the `instagramId` from the posts URL.

```
https://www.instagram.com/p/B7ISOoHne6s/
```

The `instagramId` is this bit.

```
B7ISOoHne6s
```

Then use it like this.

```jsx
<Instagram instagramId="B7ISOoHne6s" />
```
